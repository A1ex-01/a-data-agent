{
  "name": "@a-data-agent/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared base data structures and types for the a-data-agent ecosystem. Consumed by apps/web (and any future apps) to keep cross-package contracts in one place.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint configured for @a-data-agent/contracts yet)\"",
    "build": "echo \"(no build step — package is consumed as TS source via transpilePackages)\"",
    "format": "prettier --write \"src/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
