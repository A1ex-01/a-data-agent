// Public barrel for @a-data-agent/contracts.
//
// Intentionally empty for now — basic data structures will be added here
// and re-exported. Apps consume the package via:
//
//     import { ... } from "@a-data-agent/contracts"
//
// `export {}` keeps this file a module (required when the file would
// otherwise have no imports/exports) so the package can be imported
// before any real symbols are added.

export {}
